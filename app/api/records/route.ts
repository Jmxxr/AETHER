/* eslint-disable -eslint/no-explicit-any */
import {getChatGPTUser} from "../../chatgpt-auth";import {getDb} from "../../../db";import {products,sales,expenses,customers,suppliers,warranties,automations} from "../../../db/schema";const tables={products,sales,expenses,customers,suppliers,warranties,automations} as const;export async function POST(req:Request){const user=await getChatGPTUser();if(!user)return Response.json({error:"Authentication required"},{status:401});try{const{type,data}=await req.json() as {type:keyof typeof tables,data:any};if(!tables[type])return Response.json({error:"Invalid type"},{status:400});const db=getDb();const[record]=await db.insert(tables[type] as any).values(data).returning();return Response.json({record},{status:201})}catch(error){return Response.json({error:error instanceof Error?error.message:"Could not save"},{status:400})}}
/* eslint-disable @typescript-eslint/no-explicit-any */
